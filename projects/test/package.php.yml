name: 
version: 1.0.0
deps:
  jphp-core: '*'

plugins:
- App

app:
  bootstrap: index.php
  encoding: UTF-8

sources:
- src

config:
  build-dir: ./build
  vendor-dir: ./vendor
  archive-dir: ./../
  archive-format: zip